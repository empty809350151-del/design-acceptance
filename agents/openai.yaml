interface:
  display_name: "Design Acceptance"
  short_description: "Anchored Figma overlay QA with interactive HTML reports"
  default_prompt: "Use $design-acceptance to compare a Figma design and implementation screenshot, then generate an interactive multi-page HTML acceptance report using the bundled build_report.py and the approved styles and motion in references/html-output.md."
